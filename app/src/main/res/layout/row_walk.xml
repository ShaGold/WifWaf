<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    >

    <ImageView
        android:id="@+id/avatarRowWalk"
        android:layout_width="70dp"
        android:layout_height="70dp"
        tools:background="#AAA"
        />

    <TextView
        android:id="@+id/titleRowWalk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_marginLeft="10dp"
        android:layout_toRightOf="@+id/avatarRowWalk"
        android:layout_alignTop="@+id/avatarRowWalk"
        android:textStyle="bold"
        tools:text="Title"
        />

    <TextView
        android:id="@+id/cityRowWalk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_alignLeft="@+id/titleRowWalk"
        android:layout_below="@+id/titleRowWalk"
        tools:text="city"
        />

    <TextView
        android:id="@+id/lengthRowWalk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_alignLeft="@+id/cityRowWalk"
        android:layout_below="@+id/cityRowWalk"
        tools:text="length"
        />

    <TextView
        android:id="@+id/descriptionRowWalk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_alignLeft="@+id/lengthRowWalk"
        android:layout_below="@+id/lengthRowWalk"
        tools:text="default description"
        />
</RelativeLayout>